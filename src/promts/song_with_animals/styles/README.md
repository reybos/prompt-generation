# Система стилей для Song with Animals Pipeline

## Как выбрать стиль

Стиль теперь выбирается через **UI в разделе "Shorts by Song"**:

1. Перейдите в раздел **Mini Discoveries → Shorts by Song**
2. В поле **"Visual Style"** выберите нужный стиль из выпадающего списка
3. Введите текст песни
4. Нажмите **"Generate Song with Animals"**

## Доступные стили

- **`default`** - Классический детский стиль с яркими красками
- **`steampunk`** - Винтажный стимпанк стиль с механическими элементами

## Структура файлов

```
styles/
├── styleConfig.ts    # 🎯 Конфигурация и все функции стилей
├── defaultStyle.ts   # Классический стиль
└── steampunkStyle.ts # Стимпанк стиль
```

## Добавление нового стиля

1. Создайте файл `newStyle.ts` в папке `styles/`
2. Следуйте интерфейсу `VisualStyle`
3. Добавьте импорт в `styleConfig.ts`
4. Обновите функции `getStyle()` и `getAvailableStyles()`
5. Добавьте опцию в UI (HTML select и JavaScript)

## Архитектура

- **UI выбор**: Стиль выбирается пользователем через dropdown в форме
- **Передача на сервер**: Выбранный стиль передается в API запросе
- **Пайплайн**: Стиль применяется автоматически при генерации изображений
- **Консистентность**: Все изображения в группе генерируются в одном стиле

## Преимущества новой архитектуры

- ✅ **Интерактивность** - пользователь может выбирать стиль для каждого запроса
- ✅ **Гибкость** - легко переключаться между стилями без изменения кода
- ✅ **Прозрачность** - видно какой стиль используется в процессе генерации
- ✅ **Расширяемость** - простое добавление новых стилей в UI
